# SPDX-License-Identifier: MIT-0
---
# Fact-derived only. Editable CUDA settings live in group_vars/all.yml.

# Repository architecture (x86_64 or sbsa for ARM servers)
cuda_repo_arch: "{{ 'sbsa' if ansible_facts['architecture'] == 'aarch64' else 'x86_64' }}"
